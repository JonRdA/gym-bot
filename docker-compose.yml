services:
  mongo:
    image: mongo:4.4.18
    container_name: gym-bot-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    env_file:
      - .mongo.env
    volumes:
      - ./mongo-data:/data/db
